# CI Repair Lab

Frontend interativo para demonstrar classificação, roteamento, geração de patch e validação de falhas de CI.

```bash
npm install
npm run dev
```

O GitHub Actions executa `npm ci` e `npm run build`. A publicação no GitHub Pages acontece somente na `main`.
